# Database
DATABASE_URL=postgresql://nutrition:password@localhost:5432/nutrition_ai
POSTGRES_USER=nutrition
POSTGRES_PASSWORD=password
POSTGRES_DB=nutrition_ai

# API
SECRET_KEY=change-this-to-a-random-secret-key-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
CORS_ORIGINS=*
CORS_ALLOW_CREDENTIALS=false

# Model
MODEL_PATH=models/
MODEL_VERSION=v1.0.0

# Environment
ENVIRONMENT=development
DEBUG=True

# Admin panel security (optional but recommended)
# If set, `/admin` APIs require header: X-Admin-Key: <ADMIN_API_KEY>
ADMIN_API_KEY=
UI_CONFIG_PATH=data/ui_config.json

# Redis (optional, for caching/queues)
REDIS_URL=redis://localhost:6379/0

# External APIs (future)
# OPENWEATHER_API_KEY=
# PRISM_API_KEY=

# Firebase Auth (for verifying ID tokens on protected backend routes)
# Pull FIREBASE_PROJECT_ID from Firebase Console → Project Settings → General.
# FIREBASE_CREDENTIALS_PATH points at a service-account JSON downloaded from
# Project Settings → Service Accounts → "Generate new private key".
FIREBASE_PROJECT_ID=
FIREBASE_CREDENTIALS_PATH=
